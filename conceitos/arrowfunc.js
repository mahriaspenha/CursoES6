const arr = [1, 2, 4, 5, 6];

const newArr = arr.map(item => item * 2); // codigo reduzido

console.log(newArr);

// redução da function 
// quando a função recebe um parametro podemos remove-la 
// *Callback: passagem de função para outra função  

const teste = () => ({ nome: 'Mahria'});

console.log(teste());

